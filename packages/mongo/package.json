{
  "name": "@kweli-mcp/mongo",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Shared MongoDB client + the Mukoko verification-tier ladder. Consumed by every app in this monorepo that reads/writes the shared Mukoko cluster \u2014 must never drift from kweli's own lib/mongodb/client.ts and lib/verification-tiers.ts.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "mongodb": "7.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^6.0.3",
    "@types/node": "^25.9.0"
  }
}
