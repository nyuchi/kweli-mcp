{
  "name": "@kweli-mcp/telemetry",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "OpenTelemetry-shaped tracing and structured logging for Nyuchi/Mukoko workers. W3C traceparent propagation, spans, and pluggable sinks (JSON stdout, D1 agent_events, OTLP). Zero runtime dependencies so it can be vendored into any Worker or Next.js app.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "@types/node": "^25.9.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
